# Classical fixtures (captured live from ScenicRoutesFM)

Raw-but-trimmed Plex `/children?includeGuids=1` payloads for the Classical Mode test corpus.
Each *.plex.json: { name, ratingKey, album{title,parentTitle,year,guids,leafCount}, tracks[{ratingKey,index,parentIndex,title,duration,originalTitle,guids}] }.
Captured post album-merge/retitle; Le Sage is the merged 331-track box.
MB work-relation payloads (for 2a matching) land alongside as *.mb.json.
