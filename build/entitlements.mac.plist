<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Electron / V8 needs these for JIT + unsigned executable memory + dyld env. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <!-- Required to dlopen @arcanewizards/electron-media-service's native .node
       under hardened runtime + library validation. -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <!-- Outbound HTTPS for Plex, MusicBrainz, Wikipedia, Last.fm, Cover Art Archive,
       GitHub releases. Plus LAN connections to local Plex servers + headless
       Plexamp targets, which sit on http://*.local addresses. -->
  <key>com.apple.security.network.client</key>
  <true/>
</dict>
</plist>
